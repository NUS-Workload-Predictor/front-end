import React, { Component, PropTypes } from 'react';

class ReadingBoard extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (

    );
  }
}

ReadingBoard.propTypes = {

};

export default ReadingBoard;
