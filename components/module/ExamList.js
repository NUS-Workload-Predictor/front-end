import React, { Component, PropTypes } from 'react';

class ExamList extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (

    );
  }
}

ExamList.propTypes = {

};

export default ExamList;
