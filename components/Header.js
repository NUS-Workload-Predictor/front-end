import React, { Component, PropTypes } from 'react';

class Header extends Component {
  render() {
    return (
      <span>Welcome!</span>
      <button>Login</button>
    );
  }
}

Header.propTypes = {

};

export default Header;
